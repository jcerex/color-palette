# Color Palette Generator

This project helps you create color palettes from [Tailwind colors]('https://tailwindcss.com/docs/colors'), based on color theory.

Each palette you save will be stored in localstorage.

## Usage

1. Git clone this repo or download `index.html`
2. Open `index.html` in a browser
